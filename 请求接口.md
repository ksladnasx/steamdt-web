# 饰品信息查询API 文档

## 概述

本文档提供了SteamDT平台的饰品信息查询接口，包括饰品基础信息、价格查询、磨损度分析等功能。API分为两个基地址：

- **基础接口基地址**：`https://open.steamdt.com/open`
- **用户相关接口基地址**：`https://api.steamdt.com`

> **注意**：所有请求必须携带 `Authorization` 请求头。

---

## 全局公共参数

### 请求头（Header）

| 参数名        | 示例值        | 参数类型 | 是否必填 | 参数描述 |
| ------------- | ------------- | -------- | -------- | -------- |
| Authorization | Bearer apikey | string   | 是       | 认证令牌 |

### 认证方式

> Bearer Token 认证，需在请求头中附带 `Authorization: Bearer <apikey>`。其中的apikey请注册steamdt获取

---

## 接口列表

### 1. 获取饰品基础信息

> **注意**：此接口每天仅限调用一次，会返回全量的数据，建议缓存返回数据。

**接口状态**：开发中  
**请求方式**：GET  
**基地址**：`https://open.steamdt.com/open`  
**接口路径**：`/open/cs2/v1/base`

**请求示例**：

```http
GET https://open.steamdt.com/open/cs2/v1/base
Authorization: Bearer 04a77f47d70a47809ce4006c553494f2
```

**响应示例（200）**：

```json
{
	"success": true,
	"data": [
		{
			"name": "AWP | 响尾蛇 (略有磨损)",
			"marketHashName": "AWP | Pit Viper (Minimal Wear)",   //饰品在数据库中的hash名称
			"platformList": [   //该饰品在不同平台的id
				{
					"name": "C5",
					"itemId": "22313"
				},
				{
					"name": "BUFF",
					"itemId": "34123"
				}
			]
		},
	]
	"errorCode": 0,
	"errorMsg": null,
	"errorData": null,
	"errorCodeStr": null
```

---

### 2. 通过marketHashName查询饰品价格

**接口状态**：开发中  
**请求方式**：GET  
**基地址**：`https://open.steamdt.com/open`  
**接口路径**：`/open/cs2/v1/price/single`

**Query参数**：

| 参数名         | 示例值                             | 是否必填 | 说明         |
| -------------- | ---------------------------------- | -------- | ------------ |
| marketHashName | M4A4 \| Evil Daimyo (Field-Tested) | 是       | 饰品市场名称 |

**请求示例**：

```http
GET https://open.steamdt.com/open/cs2/v1/price/single?marketHashName=M4A4%20%7C%20Evil%20Daimyo%20(Field-Tested)
Authorization: Bearer <your_token>
```

**响应示例（200）**：

```json
{
  "success": true,
  "data": [...],
  "errorCode": 0,
  "errorMsg": null
}
```

---

### 3. 通过MarketHashName查询所有平台近7天均价

**接口状态**：开发中  
**请求方式**：GET  
**基地址**：`https://open.steamdt.com/open`  
**接口路径**：`/open/cs2/v1/price/avg`

**Query参数**：

| 参数名         | 示例值                             | 是否必填 | 说明         |
| -------------- | ---------------------------------- | -------- | ------------ |
| marketHashName | M4A4 \| Evil Daimyo (Field-Tested) | 是       | 饰品市场名称 |

**请求示例**：

```http
GET https://open.steamdt.com/open/cs2/v1/price/avg?marketHashName=M4A4%20%7C%20Evil%20Daimyo%20(Field-Tested)
Authorization: Bearer <your_token>
```

**响应示例（200）**：

```json
{
  "success": true,
  "data": {...},
  "errorCode": 0,
  "errorMsg": null
}
```

---

### 4. 通过检视链接查询磨损度相关数据

**接口状态**：开发中  
**请求方式**：POST  
**基地址**：`https://open.steamdt.com/open`  
**接口路径**：`/open/cs2/v1/wear`  
**Content-Type**：`application/json`

**Body参数**：

```json
{
  "notifyUrl": "",
  "inspectUrl": ""
}
```

**请求示例**：

```http
POST https://open.steamdt.com/open/cs2/v1/wear
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "notifyUrl": "",
  "inspectUrl": "steam://rungame/..."
}
```

**响应示例（200）**：

```json
{
  "success": false,
  "data": null,
  "errorCode": 4006,
  "errorMsg": "系统异常，请稍后重试"
}
```

---

### 5. 饰品不同磨损信息查看

**接口状态**：开发中  
**请求方式**：POST  
**基地址**：`https://api.steamdt.com`  
**接口路径**：`/user/skin/v1/sale-wear-detail`  
**Content-Type**：`application/json`

**Query参数**：

| 参数名    | 示例值     | 是否必填 | 说明                 |
| --------- | ---------- | -------- | -------------------- |
| timestamp | 1768451708 | 是       | 当前时间戳，自动生成 |

**Body参数**：

```json
{
  "appId": 730,
  "marketHashName": "★ Hand Wraps | Badlands (Minimal Wear)"
}
```

**请求示例**：

```http
POST https://api.steamdt.com/user/skin/v1/sale-wear-detail?timestamp=1768451708
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "appId": 730,
  "marketHashName": "★ Hand Wraps | Badlands (Minimal Wear)"
}
```

**响应示例（200）**：

```json
{
  "success": true,
  "data": {...},
  "errorCode": 0,
  "errorMsg": null
}
```

---

### 6. 获取某饰品不同平台售价

**接口状态**：开发中  
**请求方式**：GET  
**基地址**：`https://api.steamdt.com`  
**接口路径**：`/user/skin/v1/current-sell`

**Query参数**：

| 参数名    | 示例值     | 是否必填 | 说明       |
| --------- | ---------- | -------- | ---------- |
| itemId    | 489899185  | 是       | 饰品ID     |
| timestamp | 1768451708 | 是       | 当前时间戳 |

**请求示例**：

```http
GET https://api.steamdt.com/user/skin/v1/current-sell?itemId=489899185&timestamp=1768451708
Authorization: Bearer <your_token>
```

**响应示例（200）**：

```json
{
  "success": true,
  "data": [...],
  "errorCode": 0,
  "errorMsg": null
}
```

---

## 状态码说明

| 状态码 | 说明         |
| ------ | ------------ |
| 200    | 请求成功     |
| 404    | 资源未找到   |
| 4006   | 系统内部异常 |

---

## 更新记录

| 时间                | 操作人 | 说明         |
| ------------------- | ------ | ------------ |
| 2026-01-15 12:45:40 | 曉&凾  | 文档最终更新 |
| 2026-01-15 11:45:33 | 曉&凾  | 文档初次创建 |

---

**文档维护人**：曉&凾  
**最后更新时间**：2026-01-15 12:45:40